CREATE TABLE migration_x_catalog.pfl_x_schema.ingestion_config (
  ingestion_object_id       BIGINT,
  source_system_id          BIGINT,
  source_system_name        STRING,
  source_schema             STRING,
  source_object_name        STRING,
  source_object_type        STRING,
  custom_query              STRING,
  source_filter             STRING,
  load_type                 STRING,
  write_mode                STRING,
  incremental_column        STRING,
  incremental_column_type   STRING,
  incremental_end_value     STRING,
  watermark_lag_sec         INT,
  primary_key_cols          STRING,
  partition_column          STRING,
  data_read_size            BIGINT,
  file_format               STRING,
  sheet_name                STRING,
  target_catalog            STRING,
  target_schema             STRING,
  target_table              STRING,
  target_medallion_layer    STRING,
  schema_evolution_mode     STRING,
  pipeline_name             STRING,
  pipeline_id               STRING,
  created_timestamp         TIMESTAMP,
  updated_timestamp         TIMESTAMP,
  last_updated_by           STRING
) USING DELTA;
